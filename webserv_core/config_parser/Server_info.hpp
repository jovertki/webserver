#pragma once
#include <vector>
#include <iostream>

class Server_info {
private:

public:
    Server_info(std::vector<std::string>::iterator first,
                std::vector<std::string>::iterator last);
};